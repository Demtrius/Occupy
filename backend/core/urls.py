"""
URL Configuration for core app.

This module provides backward compatibility redirects to the new v1 API structure.
The actual URL patterns are now organized in the core/urls/ directory.
"""

from django.urls import path

app_name = "core"

urlpatterns = []
