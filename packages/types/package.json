{
  "name": "@occupy/types",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "sideEffects": false,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@occupy/config": "workspace:*"
  },
  "scripts": {
    "generate": "pnpm exec orval --config orval.config.ts  && pnpm exec biome format --write ./src",
    "lint": "biome lint .",
    "format": "biome format --write .",
    "typecheck": "tsc --project tsconfig.json --noEmit"
  }
}
